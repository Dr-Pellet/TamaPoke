package com.tamapoke.core.enums

/** Ported from the ShadowEnemy expanded fork's pet.h/pet.cpp personality() - purely derived, never stored directly. */
enum class PetPersonality { BALANCED, PLAYFUL, BRAVE, CALM, LAZY }
